public class line {
	// draws the lines
	public void lines() {
		/*
			 linex = new float[Int32.Parse(textBox1.Text),2];
	            liney = new float[Int32.Parse(textBox1.Text),2];
	            int c = 0;
	            Pen pen = new Pen(Color.Black);
	            Graphics g = panel1.CreateGraphics();
	            int x = 0;
	            int speed = Int32.Parse(textBox3.Text);
	            if(x + steps < xc.Length)
	            {
	                g.DrawLine(pen, xc[x]+1, yc[x]+1, xc[x + steps]+1, yc[x + steps]+1);
	                linex[c,0] = xc[x] + 1;
	                linex[c,1] = xc[x + steps] + 1;
	                liney[c,0] = yc[x] + 1;
	                liney[c,1] = yc[x + steps] + 1;
	                c++;
	                x = x + steps;
	                Thread.Sleep(speed);
	            if (x + steps >= xc.Length)
	            {
	                g.DrawLine(pen, xc[x]+1, yc[x]+1, xc[(x + steps) % xc.Length]+1, yc[(x + steps) % xc.Length]+1);
	                linex[c, 0] = xc[x] + 1;
	                linex[c, 1] = xc[(x + steps) % xc.Length] + 1;
	                liney[c, 0] = yc[x] + 1;
	                liney[c, 1] = yc[(x + steps) % xc.Length] + 1;
	                c++;  
	                x = (x + steps) % xc.Length;
	                Thread.Sleep(speed);
	            }
	            }
	            while (x != 0)
	            {
	                if(x + steps < xc.Length)
	                {
	                    g.DrawLine(pen, xc[x]+1, yc[x]+1, xc[x + steps]+1, yc[x + steps]+1);
	                    linex[c, 0] = xc[x] + 1;
	                    linex[c, 1] = xc[x + steps] + 1;
	                    liney[c, 0] = yc[x] + 1;
	                    liney[c, 1] = yc[x + steps] + 1;
	                    x = x + steps;
	                    c++;
	                    Thread.Sleep(speed);
	                }
	                else if (x + steps >= xc.Length)
	                {
	                    g.DrawLine(pen, xc[x]+1, yc[x]+1, xc[(x + steps) % xc.Length]+1, yc[(x + steps) % xc.Length]+1);
	                    linex[c, 0] = xc[x] + 1;
	                    linex[c, 1] = xc[(x + steps) % xc.Length] + 1;
	                    liney[c, 0] = yc[x] + 1;
	                    liney[c, 1] = yc[(x + steps) % xc.Length] + 1;
	                    c++;
	                    x = (x + steps) % xc.Length;
	                    Thread.Sleep(speed);
	                } 
	            }
	            g.Dispose();
	            pen.Dispose();
	            label5.Text = c.ToString();
	            place = c-1;
	            */
		}
}
